apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jdownloader-smb-media-pvc
  namespace: jovian-prod
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: smb-csi-media
  resources:
    requests:
      storage: 72Ti
  volumeName: jdownloader-smb-media
