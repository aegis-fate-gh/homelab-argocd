apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prune-smb-media-pvc
  namespace: jovian-prod
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: smb-csi-media
  resources:
    requests:
      storage: 72Ti
  volumeName: prune-smb-media
