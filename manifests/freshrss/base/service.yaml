apiVersion: v1
kind: Service
metadata:
  name: freshrss-service
  namespace: jovian-prod
spec:
  selector:
    app: freshrss
  ports:
    - port: 8090
      targetPort: freshrss-http
      protocol: TCP
      name: freshrss-http