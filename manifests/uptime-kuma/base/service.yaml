apiVersion: v1
kind: Service
metadata:
  name: uptimekuma-service
  namespace: jovian-prod
spec:
  selector:
    app: uptimekuma
  ports:
    - port: 3001
      targetPort: uptimekuma-http
      protocol: TCP
      name: uptimekuma-http
