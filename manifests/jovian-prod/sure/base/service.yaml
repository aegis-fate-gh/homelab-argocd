apiVersion: v1
kind: Service
metadata:
  name: sure
  namespace: jovian-prod
spec:
  selector:
    app: sure
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: sure-http
