apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mkvtoolnix-smb-media-pvc
  namespace: jovian-prod
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: smb-csi-media
  resources:
    requests:
      storage: 72Ti
  volumeName: plex-smb-media
